RewriteEngine On

# Ne rien réécrire si le fichier ou dossier demandé existe
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Sinon, rediriger vers index.php
RewriteRule ^ index.php [QSA,L]